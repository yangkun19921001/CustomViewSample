<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.devyk.customview">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MapActivity"
        android:configChanges="orientation|screenSize"
        >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--<activity android:name=".SVGDemo1Activity"/>-->
        <!--<activity android:name=".PathActivity"/>-->
        <!--<activity android:name=".CanvasSample"/>-->
        <!--<activity android:name=".TestActivity1"/>-->
        <!--<activity android:name=".CustomViewGroup"/>-->
        <!--<activity android:name=".FlowLayoutActivity"/>-->
        <!--<activity android:name=".MapActivity"/>-->
    </application>

</manifest>